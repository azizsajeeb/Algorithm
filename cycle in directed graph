bool dfs(int u, vector<vector<int>>& adj, vector<int>& color)
{
    color[u] = 1; // gray

    for (int v : adj[u]) {
        if (color[v] == 1)
            return true;

        if (color[v] == 0 && dfs(v, adj, color))
            return true;
    }

    color[u] = 2; // black
    return false;
}
